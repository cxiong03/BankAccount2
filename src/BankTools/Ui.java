package BankTools;

public interface Ui {
    // hacker challenge add min max options to the get number method
    int getNumber(String query, int min, int max);
    String getString(String query);
}
